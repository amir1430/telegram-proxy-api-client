name: telegram_proxy_api
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com
environment:
  sdk: ">=2.16.1 <3.0.0"

# dependencies:
#   path: ^1.8.0
dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
  mocktail: ^0.3.0

dependencies:
  dio: ^4.0.4
  equatable: ^2.0.3
